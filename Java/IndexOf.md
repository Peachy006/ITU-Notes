
In java u can use Index of to find something in a string, it will return an integer telling you the placement of the searched element.

https://www.w3schools.com/java/ref_string_indexof.asp

If it returns 6 then it means the first character is at the 7th placement since it starts from 0